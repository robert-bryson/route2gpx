version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step — static site"
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - robots.txt
  cache:
    paths: []
